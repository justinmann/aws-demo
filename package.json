{
  "name": "Node-Webiste-EB-Sample-App",
  "description": "Sample NodeJs web application",
  "version": "0.0.1",
  "private": true,
  "devDependencies": {
    "@types/express": "^4.11.1",
    "mocha": "5.0.0",
    "supertest": "3.0.0",
    "tslint": "^5.9.1",
    "typescript": "^2.7.2",
    "unit.js": "2.0.0"
  },
  "scripts": {
    "start": "npm run build && node dist/app.js",
    "build": "npm run build-ts && npm run tslint",
    "build-ts": "tsc",
    "tslint": "tslint -c tslint.json -p tsconfig.json",
    "test": "npm run build && mocha --recursive tests --exit"
  },
  "dependencies": {
    "express": "^4.16.2"
  }
}
